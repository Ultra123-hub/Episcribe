# llama-cpp-python only ships an sdist on PyPI (needs a C++ compiler to build);
# this index carries prebuilt CPU wheels for Windows/Linux/macOS instead.
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu

gradio>=4.44,<5.0
gradio_client==1.3.0
fastapi==0.115.6
starlette==0.41.3
llama-cpp-python>=0.2.90
# gradio 4.44.1's oauth module imports the deprecated HfFolder class, which
# huggingface_hub removed in its 1.0 release — stay on the last 0.x line.
huggingface_hub>=0.24,<1.0
pydantic>=2.7
faster-whisper>=1.0.3
pandas>=2.0
python-dotenv>=1.0
