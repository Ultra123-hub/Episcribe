# Model weights (downloaded, not committed — ~2.49GB)
models/*.gguf
models/*.bin

# Local database & exports — includes real encounter data, never commit
data/*.db
data/*.db-journal
data/*.db.backup
data/*.csv

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Env
.env

# Self-signed TLS cert/key for LAN HTTPS access
certs/

# OS
.DS_Store
Thumbs.db

# Keep folder structure
!data/.gitkeep
!models/.gitkeep
